rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ✅ السماح للمستخدمين المسجلين بالقراءة والكتابة الكاملة على courses_credentials
    match /courses_credentials/{docId} {
      allow read, write: if request.auth != null;
    }

    // ✅ السماح للمستخدمين المسجلين بالقراءة والكتابة على طلبات الانضمام
    match /enrollmentRequests/{docId} {
      allow read, write: if request.auth != null;
    }

    // ✅ باقي المجموعات: مفتوحة للمستخدمين المسجلين
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
